% ─────────────────────────────────────────────────────────────────────────────
%  Preamble for the IEEE conference submission.
%  Loaded after \documentclass[conference]{IEEEtran}.
%
%  Float placement is left entirely to IEEEtran. The class already sets
%  \topfraction 0.9, \bottomfraction 0.4, \textfraction 0.1,
%  \floatpagefraction 0.8, \dbltopfraction 0.8 and topnumber/bottomnumber/
%  totalnumber = 2/2/4, which are tuned for the two-column IEEE layout.
%  Overriding them produces pages that are almost entirely floats and is not
%  IEEE house style.
% ─────────────────────────────────────────────────────────────────────────────

% ── Font encoding (needed for accented characters: ą, ń, ż, é) ────────────────
\usepackage[T1]{fontenc}

% ── Mathematics ───────────────────────────────────────────────────────────────
\usepackage{amsmath,amssymb,amsfonts}

% ── Figures and tables ────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage[caption=false,font=footnotesize]{subfig}   % Fig. 1 sub-panels
\usepackage{array}                                     % tabular column types

% ── Pseudocode ────────────────────────────────────────────────────────────────
\usepackage{algorithm}
\usepackage{algpseudocode}
\algrenewcommand\algorithmicrequire{\textbf{Input:}}
\algrenewcommand\algorithmicensure{\textbf{Output:}}
\algrenewcommand\alglinenumber[1]{{\footnotesize#1}}

% Algorithm 1 spans both columns. Some versions of algorithm.sty do not define
% the starred variant, so provide it only if absent.
\makeatletter
\@ifundefined{algorithm*}{%
  \newenvironment{algorithm*}{\@dblfloat{algorithm}}{\end@dblfloat}%
}{}
\makeatother

% ── Custom notation ───────────────────────────────────────────────────────────
\newcommand{\alphah}{\hat{\alpha}}          % estimated alpha
\newcommand{\Ph}{\hat{P}}                   % empirical PDF
\newcommand{\rhoh}{\hat{\rho}}              % estimated autocorrelation
\newcommand{\VRh}{\widehat{VR}}             % estimated variance ratio
\newcommand{\Etau}{\mathbb{E}}              % expectation
